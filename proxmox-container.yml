---

- hosts:
    - proxmoxhost
  vars_prompt:
    - name: proxmox_vmid
      prompt: VM ID
      private: no
    - name: proxmox_hostname
      prompt: Hostname
      private: no
  roles:
    - proxmox-container
